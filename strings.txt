Intern is used to save on memory. It assigns strings with the same varable name the same value
in Python, so if you have a string that's repeated multiple times throughout your code (e.
g., "hello world"),
it can be assigned only once and saved in memory for later use instead of
creating new copies every time it appears again. This saves both space and processing power when working with large
creating new copies every time it appears again. This saves both space and processing power when working with large
creating new copies every time it appears. This saves both space and processing power when working with large amounts

String Declaration:
You can declare a string variable using the string keyword: